/* gtypes.h declares gbitmap_*_with_resource(), which nothing here calls; it
 * needs only the ResAppNum spelling to parse those prototypes. */
#pragma once
#include <stdint.h>
typedef uint32_t ResAppNum;
